
/**
 * This is on main branch so far
 * @param x
 * @returns 
 */

function abc(x : number) : number {
    return(1);
}

/**
 * post m3 feature commit F4
 * @param x 
 * @returns 
 */

function xyz(x : number[]) : number {
    return(1);
}
